{
  "name": "portfolio-backend",
  "version": "1.0.0",
  "description": "Backend server for portfolio website",
  "scripts": {
    "install-deps": "pip install -r requirements.txt",
    "start": "cd app && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "npm run install-deps && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
